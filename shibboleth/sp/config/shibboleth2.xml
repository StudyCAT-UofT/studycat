<SPConfig xmlns="urn:mace:shibboleth:3.0:native:sp:config"
    xmlns:conf="urn:mace:shibboleth:3.0:native:sp:config"
    clockSkew="180">

    <!--
    StudyCAT Service Provider Configuration
    Maps to IdP at idp.studycat.local
    -->

    <!-- The ApplicationDefaults element is where most of SP's SAML bits are defined. -->
    <ApplicationDefaults entityID="https://sp.studycat.local/shibboleth"
                         REMOTE_USER="eppn uid mail"
                         cipherSuites="DEFAULT:!EXP:!LOW:!aNULL:!eNULL:!DES:!IDEA:!SEED:!RC4:!3DES:!kRSA:!SSLv2:!SSLv3:!TLSv1:!TLSv1.1">

        <!--
        Controls session lifetimes, address checks, cookie handling, and the protocol handlers.
        -->
        <Sessions lifetime="28800" timeout="3600" relayState="ss:mem"
                  checkAddress="false" handlerSSL="true" cookieProps="https">

            <!--
            SessionInitiator for starting login - triggers redirect to IdP
            -->
            <SessionInitiator type="Chaining" Location="/Login" isDefault="true" id="Login">
                <SessionInitiator type="SAML2" template="bindingTemplate.html"/>
            </SessionInitiator>

            <!--
            md:AssertionConsumerService locations handle different bindings for receiving SAML assertions
            -->
            <md:AssertionConsumerService Location="/SAML2/POST" index="1"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"/>
            <md:AssertionConsumerService Location="/SAML2/POST-SimpleSign" index="2"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST-SimpleSign"/>
            <md:AssertionConsumerService Location="/SAML2/Artifact" index="3"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Artifact"/>
            <md:AssertionConsumerService Location="/SAML/POST" index="4"
                Binding="urn:oasis:names:tc:SAML:1.0:profiles:browser-post"/>
            <md:AssertionConsumerService Location="/SAML/Artifact" index="5"
                Binding="urn:oasis:names:tc:SAML:1.0:profiles:artifact-01"/>

            <!-- LogoutInitiators enable SP-initiated local or global/single logout -->
            <LogoutInitiator type="Chaining" Location="/Logout">
                <LogoutInitiator type="SAML2" template="bindingTemplate.html"/>
                <LogoutInitiator type="Local"/>
            </LogoutInitiator>

            <!-- md:SingleLogoutService locations handle single logout (SLO) protocol messages. -->
            <md:SingleLogoutService Location="/SLO/SOAP"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:SOAP"/>
            <md:SingleLogoutService Location="/SLO/Redirect" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect"/>
            <md:SingleLogoutService Location="/SLO/POST" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"/>
            <md:SingleLogoutService Location="/SLO/Artifact" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Artifact"/>

            <!-- md:ManageNameIDService locations handle NameID management protocol messages. -->
            <md:ManageNameIDService Location="/NIM/SOAP"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:SOAP"/>
            <md:ManageNameIDService Location="/NIM/Redirect" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect"/>
            <md:ManageNameIDService Location="/NIM/POST" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"/>
            <md:ManageNameIDService Location="/NIM/Artifact" conf:template="bindingTemplate.html"
                Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Artifact"/>

            <!--
            Status reporting service for monitoring
            -->
            <Handler type="Status" Location="/Status" acl="127.0.0.1 ::1"/>

            <!--
            Session diagnostic service
            -->
            <Handler type="Session" Location="/Session" showAttributeValues="true"/>

            <!--
            Discovery Service - helps with multi-IdP scenarios (not needed for single IdP setup)
            -->
            <Handler type="DiscoveryFeed" Location="/DiscoFeed"/>
        </Sessions>

        <!--
        Error handling
        -->
        <Errors supportContact="root@localhost"
            helpLocation="/about.html"
            styleSheet="/shibboleth-sp/main.css"/>

        <!--
        MetadataProvider - tells SP where to find IdP metadata
        We'll load it from a local file
        -->
        <MetadataProvider type="XML" validate="true" path="metadata/idp-metadata.xml"/>

        <!--
        TrustEngine - how to verify signatures from IdP
        -->
        <TrustEngine type="ExplicitKey"/>

        <!--
        AttributeExtractor - extracts attributes from SAML assertions
        -->
        <AttributeExtractor type="XML" validate="true" reloadChanges="false" path="attribute-map.xml"/>

        <!--
        AttributeResolver - resolves attributes
        -->
        <AttributeResolver type="Query" subjectMatch="true"/>

        <!--
        AttributeFilter - filters attributes
        -->
        <AttributeFilter type="XML" validate="true" path="attribute-policy.xml"/>

        <!--
        CredentialResolver - SP's signing and encryption credentials
        -->
        <CredentialResolver type="File">
            <Key><Path>..\certificates\sp-key.pem</Path></Key>
            <Certificate><Path>..\certificates\sp-cert.pem</Path></Certificate>
        </CredentialResolver>

    </ApplicationDefaults>

    <!--
    Security policies for different types of requests
    -->
    <SecurityPolicyProvider type="XML" validate="true" path="security-policy.xml"/>

    <!--
    Protocol provider - SAML protocol handling
    -->
    <ProtocolProvider type="XML" validate="true" reloadChanges="false" path="protocols.xml"/>

</SPConfig>
